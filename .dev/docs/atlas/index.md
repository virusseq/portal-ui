# Atlas index

- [roadmap: Google Analytics 4 migration](roadmap/google-analytics-4-migration.md): why `react-ga` can't send data to a GA4 property, the full `react-ga4` API mapping, behavior-change gaps to handle deliberately, and broader GA4 capabilities worth adopting beyond parity with current tracking
