Replaced the deprecated `@overture-stack/sqon-builder` dependency with `@overture-stack/sqon`, since sqon-builder had already been dropped from the lockfile entirely (via an in-progress `arranger-components` rc upgrade) and every import of it was broken. Backfilled after the fact: this project hadn't adopted `.dev/` session logging yet at the time this work happened.

- `package.json`, `package-lock.json`: added `@overture-stack/sqon@^1.0.0-rc.4` as an explicit direct dependency (previously only pulled in transitively via `arranger-components`)
- `components/pages/clinical/RepoTable/helper.ts`: builder calls now extract with `.toValue()` and parse an existing SQON via `SqonBuilder.from()` instead of calling the old package's default export directly
- `components/pages/clinical/RepoTable/index.tsx`, `components/pages/environmental/RepoTable/index.tsx`: type-only rename, `SQON` to `SqonNode`
- `components/pages/environmental/RepoTable/helper.ts`: removed the `ExtendedFilterKey`/`ExtendedCombinationOperator`/`ExtendedSQON` workaround types entirely, since the new package's `SqonNode` already natively supports `not-in`, which is what those types existed to work around
