Caught this project up to agentics 0.10.0 (previously synced at 0.1.0), applying the batched non-conflicting upgrade after the developer confirmed the diagnosis.

- `AGENTS.md`: restructured as the sole canonical file per the new single-dispatch-table model (agentics' `agents-md-single-dispatch-table` change): version tag, interaction parameters, critical constraints, dispatch table, memory hygiene, and initialization all moved here; added the new "surface ideas unprompted" and "sanity check requests" interaction bullets; added `review-conduct.md` to the dispatch table; recorded this project as an Overture project (confirmed with the developer) and added its dispatch line
- `CLAUDE.md`: collapsed to the new minimal stub pointing at `AGENTS.md`
- `.dev/tech-debt.md`: removed the restated tech-debt entry-format skeleton, replaced with a reference by name, per the new "don't restate this format inline" rule
- `DEVELOPMENT.md`: added the GitHub CLI prerequisite and `gh auth login` setup step, matching the current template baseline
