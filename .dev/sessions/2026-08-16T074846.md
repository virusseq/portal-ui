Researched, documented, and implemented the GA4 migration: `react-ga` (Universal Analytics only) replaced with `react-ga4` in `useTrackingContext`, verified against a real dev measurement ID, then extended with three product-spec-driven custom events (`explorer_view`, `file_download`, `facet_filter`) via a shared, extensible tracking abstraction. Also synced this project's agentics adoption, several releases behind, adopting the roadmap/atlas split while there, and found this codebase's real deployed GA IDs were scattered across four inconsistent values before narrowing scope to dev-first with the user.

- `.dev/docs/atlas/roadmap/google-analytics-4-migration.md`, `.dev/docs/atlas/index.md`: design doc covering the API mapping, behavior-change gaps, testing strategy, environment-ID reconciliation, the package-swap implementation notes, and the three-event implementation, including two Arranger findings from a peer session (an undocumented `onValueChange` prop on `Aggregations`, and `DownloadButton`'s internal, non-exported `saveTSV`), both traced to the same root cause: a plain JS component with no TypeScript props interface
- `global/hooks/useTrackingContext/index.tsx`, `types.ts`: migrated to `react-ga4`'s API, added the empty-measurement-ID guard, added a generic `trackEvent(name, params)` alongside the existing `logEvent` for GA4-native named events
- `global/hooks/useTrackingContext/events.ts`, `events.test.ts`: one small typed function per named event (`trackExplorerView`, `trackFileDownload`, `trackFacetFilter`), plus the sqon-diffing logic (`isFacetValueActive`, `getFacetActionType`, `countActiveFilters`) needed because Arranger's `isActive` flag reports per-field, not per-value, state, not precise enough for this app's multi-select facets
- `global/utils/arrangerExport.ts`, `arrangerExport.test.ts`: faithful reproduction of Arranger's internal `saveTSV` column-customizer logic (sourced from the peer session tracing real source, not re-derived), wrapping it to fire `file_download` before performing the same export
- `components/pages/clinical/index.tsx`, `environmental/index.tsx`: `explorer_view` fired on mount
- `components/pages/clinical/RepoTable/index.tsx`, `environmental/RepoTable/index.tsx`: `file_download` fired from both the custom exporter and the now-replaced `'saveTSV'` shorthand
- `components/pages/clinical/Facets.tsx`, `environmental/Facets.tsx`: `facet_filter` wired via `Aggregations`'s `onValueChange`
- `global/hooks/useTrackingContext/index.test.tsx`, `jest.config.js`: first React-rendering test in this codebase; added `@testing-library/react`/`jest-environment-jsdom` (React 17-compatible versions) and `@types/jest`
- `.eslintrc.js`: added `react-ga4` to `import/core-modules`, working around an ESLint resolver limitation with its exports-only `package.json`
- `.dev/tech-debt.md`: first two entries; the `saveTSV` duplication risk, and the broader undocumented-Arranger-prop pattern behind both events findings
- `.env.local`: dev measurement ID set; removed the two stale, unrelated IDs that were there before
- `virusseq-infra-sd4h/envs/dev/virusseq-dev/.../portal-ui/values.yaml`: dev's deployed env var updated to the same ID (uncommitted; needs a feature branch, not `main`, before pushing)
- `package.json`/`package-lock.json`: `react-ga` swapped for `react-ga4`; `@testing-library/react`, `jest-environment-jsdom`, `@types/jest` added as devDependencies
- `AGENTS.md`: synced from `0.10.0` to `0.14.0` (`synced: 4c1aed6d`); pulled in the dispatch table entries and Interaction Parameters/Critical Constraints bullets added across four releases, answered the new `roadmap_split` initialization question (yes)
- `DEVELOPMENT.md`: `.dev/docs/` bullet now describes the `atlas/` subfolder specifically, matching what's actually in it
